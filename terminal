#to be typed in terminal
sudo apt update && sudo apt upgrade -y
sudo apt install python3-venv -y
python3 -m venv yolovenv
source yolovenv/bin/activate
pip install --upgrade pip
pip install ultralytics
yolo
# Activate your venv every time before running your model:
cd ~/yolovenv
source bin/activate
yolo detect predict model=best.pt source=0
from ultralytics import YOLO
# Run your model normally
model = YOLO("best.pt")
results = model("image.jpg")

#To detect object in real time via online camera connected with my phone or webcam.
Step 1: Install IP Webcam App on Phone
On your Android phone install IP Webcam
Open the app → Start Server → it will show you an IP like:http://192.168.1.10:8080
http://192.168.1.10:8080/video
Step 2: Activate Your YOLO Virtual Environment
cd ~/yolovenv
source bin/activate
Step 3: Run YOLO on Your Phone Camera Stream
Run your detection with:
yolo detect predict model=best.pt source="http://192.168.xxx.xxx:8080/video" conf=0.25 show=True
